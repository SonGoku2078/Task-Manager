# Production environment (built app served by the Express server).
# Loaded automatically by `vite build`.
VITE_APP_ENV=production
# Empty → API calls go to the same origin that served the app (the prod server).
VITE_API_URL=

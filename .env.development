# Development & Test environment (Vite dev server).
# Loaded automatically by `vite` / `vite build --mode development`.
VITE_APP_ENV=development
# Dev/Test backend runs on port 3002 with its own database (dev.db).
VITE_API_URL=http://localhost:3002
